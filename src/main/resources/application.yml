server:
  port: 8181
logging:
  level:
    io:
     # r2dbc: DEBUG
    org:
      springframework:
        data:
        #  r2dbc: DEBUG
spring:
   application:
     name: api-gateway
   r2dbc:
     url: r2dbc:postgresql://localhost:5432/food_delivery
     username: codestorykh
     password: password
   data:
     redis:
       host: localhost
       port: 6379